name: app

on:
  pull_request:
    branches: [ master ]

jobs:
  pull_request:
      runs-on: ubutu-latest
      steps:
        - uses: actions/checkout@v2
        - uses: actions/setup-node@v1
          with:
            node-version: 12
        - run: npm ci
        - run: npm run build